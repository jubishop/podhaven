---
slug: podhaven:7566596723
shortId: PODHAVEN-4J
description: Queued episode stuck "caching" forever — background download interrupted by BGTask expiry, downloading flag never cleared
sentry-status: resolved
---

# Sentry Feedback podhaven:7566596723 — Episode stuck caching, never completes

## Sessions

### 2026-06-21T16:35:00-07:00

- Verdict (confidence: high): Episode "Experts Warn About the Intelligence
  Explosion" (GUID `FPjl`, id `147064`, podcast Dylan Curious) is stuck in
  `cacheStatus == .caching` permanently. Its `downloading` flag was set true at
  09:56:13 PT inside a background `feedRefresh` BGTask, the download was still in
  flight when iOS expired the task at 09:56:31, and the
  `CacheBackgroundDelegate` completion/failure callback was never redelivered in
  any later launch — so the flag was never cleared. It's the only episode in 3
  days of logs left at `downloading=true`. Every later `downloadToCache(147064)`
  no-ops on the `guard cacheStatus != .caching` early-return
  (`CacheManager.swift:97-101`), so it can never self-heal.
- Two root-cause gaps: (1) `AppDelegate.handleEventsForBackgroundURLSession`
  only stores the completion handler, never recreates the background URLSession
  for task reattachment; (2) no launch-time reconciliation of `downloading=true`
  rows against live `cacheManagerSession.allCreatedTasks`. A single lost
  background-session event strands an episode forever.
- ALREADY FIXED IN OPEN PR #469 (`cache-await-downloads` branch, worktree
  `worktrees/cachePR`): adds `repo.downloadingEpisodeIDs()` +
  `CacheManager.reconcileStaleDownloads()` which runs in `start()` BEFORE the
  queue/current observers and clears any `downloading=true` row absent from the
  recreated session's live tasks — exactly the 147064 strand. Also
  `cachedURL(downloadingIfNeeded:)` restarts stranded `.caching` rows, and
  `timeoutIntervalForResource=24h` bounds stalls. Regression tests:
  `CacheManagerReconcileTests` ("clears a stranded downloading flag but keeps a
  live download", "resumes a stranded download for the current episode").
  PR #469 OPEN, not draft, NOT merged (last updated 2026-06-17).
- Build 537 = commit `a2c30ee4` (main HEAD, cut 2026-06-20) predates #469 —
  that gap is why the reporter hit it. Fix = land #469, not write new code.
- Did NOT implement a duplicate fix — #469 already covers it. Verified #469's
  reconcile is a complete fix for this scenario; confirmed suggestion #2
  (AppDelegate background-completion robustness) is NOT in #469 but is the
  deliberately-deferred issue #482 (F14). #469 is green/MERGEABLE; build 537
  (main HEAD, 2026-06-20) predates the 2026-06-17 PR, hence the bug.
- Ran the 4 CacheManager* suites in worktrees/cachePR on My Mac: 40/40 green,
  incl. CacheManagerReconcileTests.reconcilesStaleDownloadsOnStart.
- Actions: filed issue #503 (the bug, backed by this feedback) and added
  `Fixes #503` to PR #469 (closingIssuesReferences confirms the link → merge
  auto-closes #503). Posted breadcrumb comments on PR #469 and issue #482
  tying this feedback to each fix. User will land #469 (+ later #482/#503)
  themselves. No app code edited by me. No linked event; 0 related Sentry
  errors for this user in the 10 min window.
- Sentry: marked RESOLVED per user (2026-06-21), ahead of #469 landing —
  tracked via issue #503 (auto-closes when #469 merges).
