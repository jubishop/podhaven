---
slug: podhaven:7633266607
shortId: PODHAVEN-5T
description: Media-services reset temporarily cleared the paused Moonshots episode from OnDeck
sentry-status: resolved
---

# Sentry Feedback podhaven:7633266607 — OnDeck cleared after media-services reset

## Sessions

### 2026-07-25T17:02:21-07:00

- Verdict: Implemented the proportional Apple-aligned fix selected after triage: rebuild invalidated audio objects and restore audio-session configuration, but do not reactivate or reload playback until a user action.
- Actions: Commit `9b591dfa` returns the interrupted episode to the front of Up Next, clears invalid OnDeck and Now Playing state, shows a native explanation directing the user to tap the episode, preserves the last persisted resume point without querying the invalid player, and emits warning telemetry with reset outcomes.
- Scope: Deliberately did not add activation retries, duplicate-reset coalescing, logical OnDeck preservation across invalid audio objects, or a generalized recovery state machine. Ready PR #542 records why that complexity is not proportional to one low-harm report and the evidence that should trigger reconsideration.
- Validation: Proved the four reset regressions failed against the previous behavior; all four now pass. The 48-test reset/loading/queue/playback-control slice passed on My Mac, and `bin/lint-swift-format` passed all 399 production Swift files.
- GitHub: Opened ready PR https://github.com/jubishop/podhaven/pull/542.
- Sentry: Resolved feedback `podhaven:7633266607` and verified the fresh remote status is `resolved`.

### 2026-07-25T15:49:49-07:00

- Verdict: High confidence that an iOS media-services reset invalidated the paused Moonshots player; the generic failure path cleared OnDeck, and both automatic reset reloads failed audio-session activation before the user manually restored the episode 34 seconds later. The user did not cause it.
- Actions: First-time triage fetched the event and reporter attachments, analyzed app session 28 and the widget window, searched same-user Sentry errors, checked current Apple and Sentry guidance, and confirmed build 547 contains PR #154 commit `9c2fde72` even though that retry does not cover activation. Analysis only.
- Follow-up: No other exact feedback ledger or matching Sentry issue group was found. Given the transient harm and manual recovery, recommend a small Apple-aligned user-action/UX fix plus recurrence telemetry rather than the full media-services recovery state machine unless frequency or harm increases.
- Side finding: The current 16.8 MB worst-case feedback attachment payload is below Sentry's 40 MB compressed-request limit, but PodHaven has separate component caps rather than one envelope-wide budget and confirms enqueue rather than delivery.
- Side action: Opened GitHub issue #540 to replace the false delivery confirmation with an immediate queued acknowledgement, preserve the form on a synchronously unavailable SDK, and avoid unverifiable progress or blocking `flush` behavior.
- Sentry: left unresolved pending user decision.
