---
slug: podhaven:7558332104
shortId: PODHAVEN-4G
description: Dragging an episode to its end auto-advances, then a lingering lock-screen scrub instantly skips the next (TBPN) episode.
sentry-status: resolved
---

# Sentry Feedback podhaven:7558332104 — Episode skip after drag to completion

## Sessions

### 2026-06-17T11:20:00-07:00

- Verdict (high confidence): The `suppressRemoteScrubCommands` 500 ms window is too short. User dragged the lock-screen scrubber of ep 145384 (Hard Fork) to the end while backgrounded (BT audio) → `finishEpisode` auto-advanced to ep 145347 (TBPN "…| Diet TBPN"). The tail of the same drag gesture arrived 632 ms later as a fresh `playbackPosition(999.78 = 145347's full duration, src 145347)` — `eventLag ~1 ms` (not stale-by-delivery), `currentTime 0:00`. Suppression window had expired ~35 ms earlier (started ~+0.1 s into the gesture at `finishEpisode`, observed offset ~535 ms), so the scrub was accepted → instant finish → skipped to ep 145370. Recurrence on a build that ALREADY has the guard (`25465a57`, ancestor of build 533 / `4777a7fa`) — incomplete fix, not stale build.
- Key code: `PlayManager.swift:121` `remoteScrubSuppressionDuration = .milliseconds(500)`; `PlayManager.swift:442` sole `suppressRemoteScrubCommands()` call (inside `finishEpisode` only — manual next/prev via `load()` gets none); scrub handler `PlayManager+Events.swift:392–436`.
- Fix implemented this session (uncommitted, user has not asked to commit): (1) `remoteScrubSuppressionDuration` 500 ms → `.seconds(1)` at `PlayManager.swift:121`; (2) start/lift debug logs in `suppressRemoteScrubCommands()`; (3) `secondsSinceEpisodeBecameCurrent` added to `logRemoteScrubDecision` (new `private(set) var onDeckBecameCurrentAt: Date?` set in `setOnDeck`, cleared in `clearOnDeck`). Deferred (not done, user chose the simple timer bump): position-based guard dropping a scrub to the new episode's full duration just after a transition — keep as backstop if this recurs. Filed as follow-up issue #488.
- Tests (`PlaybackControlsTests`): added `scrubWithCurrentEpisodeIDIgnoredPastPriorWindow` (advances 750 ms past the old window, asserts suppression still active + scrub-to-end dropped); updated `freshSeekCommandsStillWorkAfterEpisodeFinish` to advance 2 s. Regression proven: new test FAILS at 500 ms (onDeck currentTime → 8:20, scrub applied) and PASSES at 1 s; full suite green (12/12), lint clean.
- Other logs: session 18 had 0 warnings/errors. Aside: `RecommendationEngine.topRecommendations(100)` ~2.98 s on foreground (97k candidates) — unrelated perf note.
- Actions: report delivered; build/commit ancestry verified (guard in build 533); no related Sentry errors in 10-min window; no Sentry follow-ups; fix + regression test implemented and verified on My Mac.
- Sentry: resolved this session (`sentry issue resolve 7558332104`); fix committed on branch worktree-draggingToNext / PR to main, not yet shipped to TestFlight.
