---
slug: podhaven:7625926806
shortId: PODHAVEN-5Q
description: Another Great Podcast cannot open because its Acast feed returns HTTP 404
sentry-status: resolved
---

# Sentry Feedback podhaven:7625926806 — Unavailable Acast feed

## Sessions

### 2026-07-21T22:36:46-07:00

- Verdict: Fixed — non-success HTTP responses now retain their status code and response URL, and podcast feeds returning HTTP 404 or 410 surface the actionable message `This podcast's feed is no longer available.` while preserving the underlying response details for diagnostics.
- Actions: Added the regression test and proved it failed before the fix, centralized typed HTTP response validation for production and test sessions, passed 15 `PodcastFeedTests`, 118 related network-facing tests across eight suites, `bin/lint-swift-format`, and a warning-free My Mac build. Committed as `570bec1e` and opened ready PR [#537](https://github.com/jubishop/podhaven/pull/537).
- Sentry: Marked PODHAVEN-5Q resolved immediately after opening the ready PR.

### 2026-07-21T21:51:28-07:00

- Verdict: High confidence — Apple search still advertises the reported Acast URL, but the publisher endpoint returns HTTP 404 `Show not found`; all four reporter attempts failed at feed fetch with `NSURLErrorDomain -1011`. The feed is unavailable upstream, while PodHaven obscures the actionable HTTP status behind a generic error.
- Actions: Fetched the feedback event and reporter app/widget attachments, found no same-user Sentry errors in the preceding 30 minutes, analyzed app session 99, correlated build 539 to commit `5477072c`, checked project history for a known fix, and verified the current Apple lookup plus Acast response. Analysis only; no application code changed.
- Sentry: Left unresolved in this ledger pending the user's explicit resolution decision; Sentry automatically set the live issue status to ignored five seconds after submission.
