---
pr: 461
title: Add manual on-device episode transcription
branch: worktree-manualTranscripts
base: main
repo: jubishop/podhaven
---

# PR 461 review ledger

## Findings

### F1 — await the audio download instead of sleep-polling
- source_thread_id: PRRT_kwDONT4Ems6JW8fX (line 187), PRRT_kwDONT4Ems6JW9MO (line 192)
- concern: `cachedAudioURL` polled the repo every 2s up to 150 times; owner wants an awaited download.
- status: fixed
- fixed_at: TranscriptionProcessor (process), CacheManager.cachedURL(downloadingIfNeeded:), CacheBackgroundDelegate
- resolution: added a per-episode AsyncLatch completion registry in CacheManager opened by the delegate on every terminal path (keyed off taskDescription so a deleted row still unblocks); processor now awaits it. Deleted the poll loop + constants + sleeper dep.

### F2 — event-drive the drain loop, drop the poll/wait structure
- source_thread_id: PRRT_kwDONT4Ems6JW8up (line 148), PRRT_kwDONT4Ems6JW86j (line 109)
- concern: `drain()` + `waitForWork()` was a poll/wait dance; fold processing into the stream loop, one-at-a-time.
- status: fixed
- fixed_at: TranscriptionProcessor.drain
- resolution: `drain()` is now `for await _ in $episodeIDs.stream() { drainUntilEmpty() }`; deleted `waitForWork()`. Serial, event-driven; shares drainUntilEmpty with the background task.

### F3 — CodeRabbit rate-limit walkthrough comment
- source_comment_id: IC_kwDONT4Ems8AAAABGCMjZw
- concern: auto-generated CodeRabbit rate-limit notice; no actionable content.
- status: skipped
- resolution: no code change; minimized.

### F4 — push the transcription test seam to the OS-integration level
- source_thread_id: PRRT_kwDONT4Ems6JW-7Q (line 35)
- concern: the `Transcribing`/`FakeTranscriber` seam is too high; fake `SpeechTranscriber`/`SpeechAnalyzer` at the OS boundary with protocols the real types conform to; document in AGENTS.md.
- status: fixed
- fixed_at: Transcriptions/Protocols + Extensions, Transcriber, AGENTS.md, transcription test fakes/helpers
- resolution: removed `Transcribing`/`FakeTranscriber`; added `SpeechAnalyzing`/`SpeechTranscribing`/`SpeechTranscriptionResult`/`SpeechModelManaging` (real Speech types conform via extension, mirroring AVPlayable/AVPlayableItem incl. the analyzer's `as? SpeechTranscriber` downcast and a protocol for the opaque `SpeechTranscriber.Result`). `Transcriber` now concrete + unit-tested via fakes (mapping/trim/empty-filter, locale-unsupported, install). Owner chose scope = transcribe + model-install. Documented the seam rule in AGENTS.md.

## Sessions

- 2026-06-13: /prfix. F1 (AsyncLatch download await), F2 (event-driven drain), F4 (OS-level test seam: SpeechAnalyzer/SpeechTranscriber protocols + fakes, AGENTS.md), F3 (CR notice minimized). Full suite green (1592 tests).
