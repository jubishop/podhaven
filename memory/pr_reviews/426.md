---
pr: 426
title: Save the whole podcast series whenever an unsaved episode is persisted
branch: worktree-refreshOnLikeEpisode
base: main
repo: jubishop/podhaven
---

# PR Review #426

0 Critical, 7 Warning, 8 Nit | 2 fixed, 1 skipped, 12 withdrawn, 0 pending | last session 2026-06-04

## Sessions

### 2026-06-04T22:08:33-07:00

- User follow-up: inlined getOrCreatePodcastEpisodeSavingSeries() into UnsavedPodcastEpisode.swift and deleted the UnsavedPodcastEpisode+Series.swift split file (F15) — the type's file is ~100 lines, so an extension-only file was unjustified.
- AGENTS.md: added "Keep every Swift file under 1000 lines" and tied extension-only file splits to that limit.
- Added two branch tests for the materializer: already-saved short-circuit (asserts no feed fetch) and mediaGUID-miss fallback (asserts series + lone episode). RowActionTests now 6/6 green; lint clean; build warning-free.

### 2026-06-04T21:59:26-07:00

- Fresh code-only review independently confirmed the PR is clean and well-tested (matches the 21:22 session).
- Re-verified all 14 team-review findings against current code; the decisive fact is the existing `UnsavedPodcast.getOrCreatePodcast()` (Podcast.swift:132) — same existence check + post-parse redirect re-check + insertSeries.
- F12 (log the mediaGUID-miss fallback) fixed in PR; RowActionTests green, lint clean, build warning-free.
- F8 (consolidate onto getOrCreatePodcast) skipped — owner kept the deliberate split.
- Withdrew F1–F7, F9–F11, F13, F14: each is by-design, idempotent/non-corrupting, or consistent with the established getOrCreatePodcast/insertSeries/subscribe convention.

### 2026-06-04T21:41:29-07:00

- team-review: 14 findings written pending for /review

### 2026-06-04T21:22:07-07:00

- Fresh code-only review of the whole diff: no actionable findings.
- Verified edge cases hold: already-saved short-circuit, feed-pull failure fallback, acted-on episode missing from feed window, active filter (allEntries is unfiltered), bulk vs single paths, duplicate-mediaGUID safety.
- Confirmed the in-memory (PodcastDetail) vs feed-pull (single-episode surfaces) split is deliberate and consistent; SearchDiscovery/EpisodeDetail correctly route through the feed-pull materializer.
- Considered extracting a shared "upsert list + match + fallback" helper; declined — overlap is ~2 lines with differing fallbacks, and AGENTS.md prefers inlined sequences over one-call-site helpers.
- PR feedback: no review threads/reviews; only CodeRabbit walkthrough summary comment (accurate, left as-is). CI green.

## Findings

### F15: Unjustified extension-only split file for a small type  (fixed)

- **severity:** Nit
- **architecture:** true
- **status:** fixed
- **location:** PodHaven/Database/Models/UnsavedPodcastEpisode.swift
- **concern:** the materializer lived in a separate UnsavedPodcastEpisode+Series.swift extension file though the type's own file is ~100 lines
- **resolution:** inlined the method into UnsavedPodcastEpisode.swift, deleted the split file, and added a 1000-line file limit to AGENTS.md gating extension-only splits
- **fixed_at:** 2026-06-04

### F12: Silent mediaGUID-miss fallback lacks logging  (fixed)

- **severity:** Nit
- **status:** fixed
- **location:** PodHaven/Database/Models/UnsavedPodcastEpisode.swift (was UnsavedPodcastEpisode+Series.swift)
- **concern:** episode-predates-feed-window fallback returned with no log while the parse-failure branch logged
- **resolution:** added `Self.log.info` with mediaGUID, pulled count, feedURL before the lone-episode upsert, per AGENTS.md guard-logging guidance; covered by a new regression test
- **fixed_at:** 2026-06-04

### F8: Save-whole-series policy split across strategies and call sites  (skipped)

- **severity:** Warning (reframed Refactor — current code is correct)
- **architecture:** true
- **status:** skipped
- **location:** PodHaven/Database/Models/UnsavedPodcastEpisode+Series.swift, PodHaven/Database/Models/Podcast.swift:132
- **concern:** chokepoint is a third feed-pull-save-series impl alongside UnsavedPodcast.getOrCreatePodcast(); could delegate to it
- **resolution:** owner kept the deliberate split — episode-level fallbacks differ and idempotent upsert suits the action path; AGENTS.md prefers inlining over premature abstraction

### F1: Existing partial podcasts never get series backfill on later saves  (withdrawn)

- **severity:** Warning
- **architecture:** true
- **status:** withdrawn
- **location:** PodHaven/Database/Models/UnsavedPodcastEpisode+Series.swift:18-20
- **concern:** alreadySaved short-circuit skips feed pull for partial podcasts
- **resolution:** out of scope — already-saved podcasts are not refetched by design (PR body + getOrCreatePodcast draw the same boundary); legacy partials heal via refresh, and the PR no longer creates new partials

### F2: No per-feedURL in-flight coalescing on first-save materialization  (withdrawn)

- **severity:** Warning
- **status:** withdrawn
- **location:** PodHaven/Database/Models/UnsavedPodcastEpisode+Series.swift:15-39
- **concern:** concurrent calls can both parse + upsert
- **resolution:** upsertPodcastEpisodes is idempotent on feedURL (no duplicate rows/corruption); getOrCreatePodcast has the identical non-coalesced TOCTOU. Worst case is one redundant fetch under double-tap

### F3: alreadySaved guard loads entire series via podcastSeries  (withdrawn)

- **severity:** Warning
- **status:** withdrawn
- **location:** PodHaven/Database/Models/UnsavedPodcastEpisode+Series.swift:18-20
- **concern:** existence check hydrates all episode rows for a boolean
- **resolution:** identical to getOrCreatePodcast():135 — the established idiom; no lighter existence probe exists in the repo. Not a divergence introduced by this PR

### F4: Single-episode surfaces block on full RSS fetch before action completes  (withdrawn)

- **severity:** Warning
- **architecture:** true
- **status:** withdrawn
- **location:** PodHaven/Database/Models/UnsavedPodcastEpisode+Series.swift:22-38
- **concern:** synchronous feed parse on the user-action critical path
- **resolution:** explicit PR intent ("ANYTIME ANYWHERE ... save the entire series"); getOrCreatePodcast also parses synchronously. Lone-episode fallback covers offline

### F5: First persist upserts every RSS item with no episode cap  (withdrawn)

- **severity:** Warning
- **status:** withdrawn
- **location:** PodHaven/Database/Models/UnsavedPodcastEpisode+Series.swift:35-38
- **concern:** no window limit on first-import upserts
- **resolution:** saving the whole series is the goal; matches the insertSeries/subscribe write path used across the app. Capping contradicts intent

### F6: PodcastDetailViewModel re-upserts full list while state stays unsaved  (withdrawn)

- **severity:** Warning
- **status:** withdrawn
- **location:** PodHaven/Views/Podcasts/Models/PodcastDetailViewModel.swift:53-60
- **concern:** rapid actions before observation transitions repeat O(n) upserts
- **resolution:** writes are idempotent and the window is brief; eager .saved transition adds complexity/risk for a minor, non-corrupting cost

### F7: persistPodcastEpisode mediaGUID-miss fallback re-enters feed-pull materializer  (withdrawn)

- **severity:** Nit
- **status:** withdrawn
- **location:** PodHaven/Views/Podcasts/Models/PodcastDetailViewModel.swift:58-59
- **concern:** fallback may network-pull instead of direct upsert
- **resolution:** edge case (stale episode not in allEntries) is correctly and more robustly handled; not a defect, micro-opt not worth it

### F9: Series materializer omits post-parse redirect lookup  (withdrawn)

- **severity:** Nit
- **architecture:** true
- **status:** withdrawn
- **location:** PodHaven/Database/Models/UnsavedPodcastEpisode+Series.swift:18-25
- **concern:** no updatedFeedURL re-check after parse (unlike getOrCreatePodcast)
- **resolution:** safe precisely because the new code uses idempotent upsert (feedURL upsert collapses the redirect collision — no duplicate row); the sibling needs the re-check only because it inserts. Coherent design

### F10: Duplicate unsaved bulk-upsert logic in PodcastDetailViewModel  (withdrawn)

- **severity:** Nit
- **architecture:** true
- **status:** withdrawn
- **location:** PodHaven/Views/Podcasts/Models/PodcastDetailViewModel.swift:53-60, 183-194
- **concern:** persistPodcastEpisode and selectedPodcastEpisodes both gather allEntries + batch-upsert
- **resolution:** ~2 lines of overlap with divergent post-processing (single match vs interleaved selection); AGENTS.md prefers inlining

### F11: Display-layer getOrCreatePodcastEpisode may trigger network I/O  (withdrawn)

- **severity:** Nit
- **architecture:** true
- **status:** withdrawn
- **location:** PodHaven/Database/DisplayModels/ListedEpisode.swift:135-139
- **concern:** name does not signal possible network on the unsaved arm
- **resolution:** the whole getOrCreate* family (incl. getOrCreatePodcast) does feed I/O under this name; consistent. Rename would churn the EpisodeDisplayable protocol disproportionately

### F13: Series materializer does not honor task cancellation  (withdrawn)

- **severity:** Nit
- **status:** withdrawn
- **location:** PodHaven/Database/Models/UnsavedPodcastEpisode+Series.swift:22-33
- **concern:** PodcastFeed.parse runs without Task.checkCancellation
- **resolution:** action Tasks (ManagingEpisodes) are fire-and-forget Task {} — not lifecycle-cancellable, so checks would not fire; getOrCreatePodcast does not check either

### F14: New podcasts use upsertPodcastEpisodes instead of insertSeries  (withdrawn)

- **severity:** Nit
- **status:** withdrawn
- **location:** PodHaven/Database/Models/UnsavedPodcastEpisode+Series.swift:35-38
- **concern:** per-row upsertAndFetch instead of insertSeries on a known-new podcast
- **resolution:** upsert is the deliberate race-safe/idempotent choice for a hot action path; insertSeries would be more fragile under the concurrency F2 flags. Subsumed by F8 if ever consolidated
