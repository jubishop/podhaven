---
pr: 440
title: "⚡️ perf(recommendations): keep playback ticks out of candidate observation regions"
branch: worktree-embedCandEpisObs
base: main
repo: jubishop/podhaven
---

# PR Review #440

0 Critical, 0 Warning, 0 Nit | 0 fixed, 0 skipped, 0 pending | last session 2026-06-09

## Sessions

### 2026-06-09T13:58:00-07:00

- Fresh review (single perf commit adding `playbackStarted` + migration v52). Zero findings; no fixes needed.
- Verified: all three `currentTime` writers keep the flag in sync (conditional boundary-only assignment in `updateCurrentTime`/`updatePlayback`, explicit clear in `markFinished`); insert/upsert/reset paths derive it in `UnsavedEpisode.init`; RSS upserts touch no playback columns.
- Backfill SQL `currentTime > 0` matches Swift `seconds > 0` across NULL/negative/invalid-CMTime edges (CMTime stores as REAL seconds, NULL when invalid). Probed a cross-column CHECK as hardening: SQLite validates ADD COLUMN CHECKs against existing rows, so it fails pre-backfill — not viable.
- No consumer regressions: Unfinished list and UpNext hydration keep per-tick progress via `currentTime` in `ListableEpisode` projections; in-memory `EpisodeFoundational.started` uses the same derivation.
- Touched suites pass locally (9 tests); CI fully green. PR feedback was only a CodeRabbit rate-limit notice (no review ran), nothing to reconcile.

## Findings

(none)
