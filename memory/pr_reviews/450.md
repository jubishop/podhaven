---
pr: 450
title: "✨ feat(settings): add separate podcast art setting for up next recommendations"
branch: worktree-podcastOrEpisodeImage
base: main
repo: jubishop/podhaven
---

# PR Review #450

0 Critical, 0 Warning, 1 Nit | 1 fixed, 0 skipped, 0 pending | last session 2026-06-12

## Sessions

### 2026-06-12T22:00:53-07:00

- Third re-review concurred: no new findings. Independently re-verified drift-proof Bool encoding (migration's JSONEncoder/`data(forKey:)` matches DefaultsStorable Codable path), idempotent guards, startup order (AppDelegate.didFinishLaunching → bootstrap → initializeAppDB → Schema.migrate before SwiftUI body resolves userSettings), and the deliberate `true` default that preserves the prior combined-setting behavior (unlike v40's intentional `false` for OnDeck). Widget remains queue-only (no recommendations consumer). No reactivity regression: `@DynamicInjected` plain read matches the pre-existing queue toggle.
- CodeRabbit completed its review this round (e34e5cbf..8ae0b0db) with no actionable comments — supersedes prior "rate-limited, never reviewed" note. No threads/reviews.

### 2026-06-12T21:34:19-07:00

- Re-review concurred with prior session: no new findings. Re-verified v57 (idempotent, drift-proof JSON encode/decode matching DefaultsStorable; guards new-key-present and old-key-absent) and startup order (AppLauncher.bootstrap forces initializeAppDB → Schema.migrate before any userSettings resolve). `as!` defaults cast in v57Tests matches v32/v40 precedent (not a deviation). PR feedback: only CodeRabbit summary (rate-limited, never reviewed); no threads/reviews.

### 2026-06-12T20:38:45-07:00

- Fresh review: migration v57 clones proven v40 pattern; startup ordering verified safe (bootstrap runs migrations before userSettings resolves); widget queue path correctly keeps old key. No CodeRabbit actionable comments; no threads/reviews.
- F1 fixed in PR: moved "for Recommendations" artwork toggle from Recommendations section into Up Next section beside "in Queue". swift-format lint + My Mac build green.

## Findings

### F1: Up Next artwork toggles split across two Settings sections  (fixed)

- **severity:** Nit
- **status:** fixed
- **location:** PodHaven/Views/Settings/SettingsView.swift
- **concern:** queue and recommendations artwork toggles lived in different Settings sections
- **resolution:** moved "for Recommendations" toggle into "Up Next" section between "in Queue" and "for Now Playing"; all three artwork toggles now grouped together
- **fixed_at:** 2026-06-12
