---
pr: 469
title: Await in-flight cache downloads + reconcile stranded download flags
branch: cache-await-downloads
base: main
repo: jubishop/podhaven
---

# PR Review #469

0 Critical, 7 Warning, 2 Refactor, 2 Nit | 10 fixed, 1 skipped | last session 2026-06-15

## Sessions

### 2026-06-15T09:36:56-07:00

- Fresh round (code-only): F11 (cachedURL registers its latch late — after ensureDownloadIsActive — so the success signal from didFinishDownloadingTo can fire into an empty slot while the task still lingers in allTasks until the later didCompleteWithError(nil); the F8 liveness re-check then sees it "live" and awaits a fresh latch that never opens → permanent hang; same path also leaks a never-opened latch on the skip-wait branch). Distinct from F5 (cross-attempt premature nil, failure path) and F8 (reattach premature nil) — different path, different/worse outcome (hang).
- Intake: all 7 PR threads already resolved; both CodeRabbit reviews minimized; closing inventory empty. CodeRabbit's withdrawn attempt-identity concern (F5) reasons about didCompleteWithError's synchronous tail, not the success/didFinishDownloadingTo late-registration window — F11 not covered.
- F11 decided fix-in-PR and fixed: didFinishDownloadingTo clears the downloading flag last; cachedURL gates the wait on that flag (ordered-before-signal) instead of task liveness. Author accepted the gate after tightening it to one repo.episode read per non-await path (replacing the hasLiveDownloadTask scan; cheaper on cached/failure paths). Production fix verified (40 cache tests green, build + lint clean) and pushed (3ebf25dd).
- Regression test requestMidCompletionDoesNotRestartDownload added (separate commit): gates loadEpisodeAsset mid-completion, asserts a concurrent downloadToCache no-ops — proven red on the pre-reorder delegate ordering (1/32 failed), green after (41 cache tests). The hang itself is untestable (mid-cachedURL transition needs forbidden injection), like F4/F7.

### 2026-06-15T08:52:36-07:00

- Fresh round (code-only): F8 (reattached-after-relaunch download has a live task but no in-memory latch → cachedURL skips the wait and returns a premature nil; lines 188-190 comment is wrong for that case).
- Intake: new open CodeRabbit review → F9 (URL-mismatch early-return strands the downloading flag, same class as F7 on a different path). New open author thread (line 104) → F10 (route giving-up cleanup through didCompleteWithError instead of duplicating). F1-F7 threads all resolved.
- All three decided fix-in-PR and fixed. F8: cachedURL now adopts/creates the latch then re-checks liveness (race-free, single-location; chose this over reconcile re-arm, which can't be regression-tested without exposing internals and leaves a launch-window race). F9+F10: extracted clearDownloadState(for:) helper (author chose the helper over cross-calling didCompleteWithError); called from move-failure (F7), URL-mismatch (F9), and didCompleteWithError paths.
- Regression tests red→green: awaitsReattachedDownloadWithoutLatch (F8); strengthened mismatchedURLRefused to assert .uncached (F9). All 4 CacheManager* suites green on My Mac (40 tests), lint clean, build zero warnings.
- Follow-up (author Q): removed downloadToCache's eager latch registration — F8's get-or-create in cachedURL made it redundant, and it pre-allocated a latch for every observer-driven download nobody awaits. cachedURL is now the sole latch registrar+awaiter; updated the field/await/signal comments + the F8 test comment to match. Cache suites still green (40).

### 2026-06-14T23:34:09-07:00

- Fresh round (CacheManager/delegate): F7 (move-failure strands downloading flag) + re-surfaced F5's residual. Both independently raised by a fresh CodeRabbit review (rate-limit cleared since last session). All 4 older threads already resolved.
- F7 fixed in PR: the non-async didFinishDownloadingTo move-failure catch now clears progress + (in a Task) the downloading flag before signaling. No regression test — non-async URLSession override isn't harness-reachable (same as F4).
- F5 skipped (author decision, no follow-up): comment corrected to drop the false "can never resolve a later one" guarantee. Race is failure-path-only, ~µs synchronous window behind a scheduling hiccup; worst case is a premature nil from a future #461 awaiter, recoverable on retry. Discussed window-narrowness + downside on the thread.
- Verified: CacheManager{Await,Reconcile,ColdLaunch,} suites green on My Mac (39 tests), zero warnings; swift-format clean.

### 2026-06-14T19:16:28-07:00

- Fresh round: F1 (latch deadlock), F4 (move-failure signal gap). Intake: F2 (line 85 thread), F3 (lines 164/244 threads).
- All 4 fixed in PR. F1/F2 have regression tests proven red on old code, green after. F4 untestable (harness never invokes the non-async delegate override). Await API has no in-repo consumer (deliberate, split from #461) — not a finding.

### 2026-06-14T22:36:23-07:00

- Fresh round: F5 (monotonic latch reused across download attempts → spurious nil), F6 (fake never prunes completed tasks → reconcile/live-task assumption untested). Both fixed in PR.
- Intake: CodeRabbit rate-limited (no bot review). 4 author threads (F2/F3 resolved; taskDescription question left open per prior note). Re-confirmed await API is deliberate staging for #461 — F-A not raised. Cleaned stray tags from ledger tail.
- F5 redesigned per author feedback (rejected an interim re-check loop): one AsyncLatch per download attempt, registered by downloadToCache and opened+removed by the delegate, instead of a shared monotonic+refcounted latch. Dropped the loop/refcount entirely.
- F6 makes the fix testable: pruning lets a second cachedURL attempt resolve its own task. New characterization test recoversWithFreshDownloadAfterFailure; skipped a strict red-on-old F5 test (deterministic drain-window overlap is impractical, per author OK).

## Findings

### F11: cachedURL can hang awaiting a latch the success callback already signaled  (fixed)

- **severity:** Warning
- **status:** fixed
- **location:** PodHaven/Cache/CacheManager.swift cachedURL; CacheBackgroundDelegate didFinishDownloadingTo
- **concern:** cachedURL registered its latch after ensureDownloadIsActive and gated the wait on task liveness, so a success signal firing into the empty slot (task still in allTasks until the later didCompleteWithError) left it awaiting a fresh latch that never reopened → permanent hang; distinct from F5 (cross-attempt nil) and F8 (reattach nil)
- **resolution:** didFinishDownloadingTo clears the downloading flag last (after updateCachedFilename); cachedURL registers the latch then gates the wait on that flag (written before the signal) instead of task liveness — still-downloading ⇒ signal still coming and opens the registered latch, otherwise read the result. Removes the skip-wait latch leak; tightened to one repo.episode read per non-await path. Regression test requestMidCompletionDoesNotRestartDownload (gates asset load mid-completion, asserts a concurrent downloadToCache no-ops) red on the old ordering, green after; 41 cache tests green. The hang itself isn't deterministically reproducible without forbidden test-only injection (a mid-cachedURL state transition) — same bucket as F4/F7.
- **fixed_at:** 2026-06-15

### F1: Concurrent same-episode awaiters can deadlock  (fixed)

- **severity:** Warning
- **status:** fixed
- **location:** PodHaven/Cache/CacheManager.swift cachedURL/signalDownloadComplete
- **concern:** cachedURL defer removed the shared latch on any return, orphaning a still-waiting concurrent caller's latch
- **resolution:** refcounted DownloadLatch (waiters count) keeps the shared latch registered until the last awaiter leaves; signalDownloadComplete reads entry.latch. Regression test cancelledAwaiterDoesNotStrandConcurrentAwaiter (red on old code, green after).
- **fixed_at:** 2026-06-14

### F2: reconcileStaleDownloads not awaited before observations start  (fixed)

- **severity:** Warning
- **status:** fixed
- **location:** PodHaven/Cache/CacheManager.swift start()/reconcileStaleDownloads
- **concern:** start() fired reconcile as a detached Task racing the currentEpisode observer, so a stranded download could be skipped as "already caching" then reconciled away
- **resolution:** reconcileStaleDownloads is now async and awaited before the observers subscribe (one ordered Task in start()). Regression test resumesStrandedCurrentEpisodeOnStart (red on old code, green after).
- **fixed_at:** 2026-06-14
- **source_thread_id:** PRRT_kwDONT4Ems6Jc3yU

### F3: cachedURL/ensureDownloadIsActive issue many repo.episode round-trips  (fixed)

- **severity:** Refactor
- **status:** fixed
- **location:** PodHaven/Cache/CacheManager.swift cachedURL/ensureDownloadIsActive
- **concern:** ensureDownloadIsActive re-read the episode cachedURL had just fetched
- **resolution:** cachedURL keeps the post-download fetch and passes the Episode into ensureDownloadIsActive(_:), dropping one DB round-trip. Reads that observe state across await points are intentionally kept.
- **fixed_at:** 2026-06-14
- **source_thread_id:** PRRT_kwDONT4Ems6Jc4cS
- **source_thread_id2:** PRRT_kwDONT4Ems6Jc4p6

### F4: temp-move failure in non-async didFinishDownloadingTo skips the latch signal  (fixed)

- **severity:** Nit
- **status:** fixed
- **location:** PodHaven/Cache/CacheBackgroundDelegate.swift didFinishDownloadingTo (non-async)
- **concern:** the non-async override returned on moveItem failure before scheduling the async work, so the defer-based completion signal never ran and an awaiter could hang
- **resolution:** signal completion in the catch (via episodeID(for:)) before returning. No regression test: the fake's finishDownload invokes the async delegate method directly, so the non-async override is never exercised by the harness.
- **fixed_at:** 2026-06-14

### F5: Cross-attempt latch resolution — stale callback resolves a newer attempt early  (skipped)

- **severity:** Warning
- **architecture:** true
- **status:** skipped
- **location:** PodHaven/Cache/CacheManager.swift:62-69
- **concern:** downloadLatches is episode-keyed, so in principle attempt-1's terminal callback can open+remove attempt-2's latch
- **resolution:** comment corrected to drop the false "can never resolve a later one" guarantee and state the single-in-flight-attempt-per-episode assumption. The race is left unfixed by author decision (no follow-up): the harmful variant is failure-path only and the signal fires in a synchronous tail right after the flag clears, so a replacement attempt would need attempt-1's thread preempted across two serialized DB writes in a ~µs window — vanishingly rare. If it ever fires (only once #461 adds a consumer), the sole effect is a premature nil from cachedURL; attempt 2 keeps downloading and caches normally, recoverable on retry. No data loss/corruption/stuck state.
- **source_thread_id:** PRRT_kwDONT4Ems6Je2r8

### F6: Fake session never prunes completed tasks, so the reconcile assumption is untested  (fixed)

- **severity:** Nit
- **status:** fixed
- **location:** PodHaven/Preview Content/Fakes/FakeDataFetchable.swift (finishDownload/failDownload)
- **concern:** real URLSession drops completed tasks from allTasks; the fake only appended, so reconcile/hasLiveDownloadTask never saw a task vanish
- **resolution:** finishDownload/failDownload now remove the task after the delegate call, mirroring the real session. This also lets a second cachedURL attempt resolve its own task (covered by recoversWithFreshDownloadAfterFailure).
- **fixed_at:** 2026-06-14

### F7: Move-failure path strands the downloading flag  (fixed)

- **severity:** Warning
- **status:** fixed
- **location:** PodHaven/Cache/CacheBackgroundDelegate.swift:99-124 (non-async didFinishDownloadingTo catch)
- **concern:** safe-temp move failure returned after signaling the latch but never cleared downloading/progress, so the episode stayed .caching with no live task until next launch's reconcile.
- **resolution:** the catch now clears download progress and, in a Task, clears the downloading flag before calling signalDownloadComplete (signal nested after the await, so the flag is cleared first; awaiter still unblocked if the write throws). No regression test: the fake drives the async `any DataFetchable` override directly, so the non-async URLSession override (which owns the safe-temp move) isn't harness-reachable — same limitation noted for F4.
- **fixed_at:** 2026-06-15
- **source_thread_id:** PRRT_kwDONT4Ems6Je2r3

### F8: Reattached download after relaunch isn't awaited — premature nil  (fixed)

- **severity:** Warning
- **status:** fixed
- **location:** PodHaven/Cache/CacheManager.swift cachedURL
- **concern:** downloadLatches is in-memory; a background download reattached after relaunch had a live task but no latch, so cachedURL skipped the wait and returned a premature nil while the download was still in-flight
- **resolution:** cachedURL now adopts the existing latch or registers one (get-or-create), then re-checks hasLiveDownloadTask before awaiting (so a just-finished task whose signal found no latch isn't awaited); comment corrected. Chose this over reconcile re-arm — that can't be regression-tested without exposing internals and leaves a launch-window race. Regression test awaitsReattachedDownloadWithoutLatch (red→green).
- **fixed_at:** 2026-06-15

### F9: URL-mismatch early-return strands the downloading flag  (fixed)

- **severity:** Warning
- **status:** fixed
- **location:** PodHaven/Cache/CacheBackgroundDelegate.swift didFinishDownloadingTo (async) URL-mismatch guard
- **concern:** the URL-mismatch defensive return signaled the latch (defer) but never cleared downloading/progress, leaving the episode .caching with no live task
- **resolution:** calls the new clearDownloadState(for:) helper before returning. Regression: strengthened mismatchedURLRefused to set downloading=true and assert .uncached (red→green).
- **fixed_at:** 2026-06-15
- **source_review_id:** PRR_kwDONT4Ems8AAAABDCdsug

### F10: Duplicate giving-up cleanup; extracted clearDownloadState helper  (fixed)

- **severity:** Refactor
- **status:** fixed
- **location:** PodHaven/Cache/CacheBackgroundDelegate.swift (move-failure, URL-mismatch, didCompleteWithError)
- **concern:** each "resolved episode but giving up" early-return re-implemented clearProgress + clearFlag + signal; the duplication let F9 drift
- **resolution:** extracted private clearDownloadState(for:) (clear progress + flag, log on failure), called from all three give-up paths — replacing F7's hand-rolled block. Author chose the helper over cross-calling didCompleteWithError.
- **fixed_at:** 2026-06-15
- **source_thread_id:** PRRT_kwDONT4Ems6JoPj3

## Notes

- line 105 thread (PRRT_kwDONT4Ems6Jc4Pa): "is taskDescription guaranteed to be an episode id?" — yes, downloadToCache sets it to String(episode.id.rawValue) and episodeID(for:) guards non-parseable. Answered and resolved on author's request (open-count now 0).
- await API (cachedURL/ensureDownloadIsActive/hasLiveDownloadTask/downloadLatches) has no in-repo consumer; deliberate per PR body (split from #461). Not raised as a finding.
