---
pr: 473
title: "⚡️ perf(search): compute podcast-context vector once per feed"
branch: perf/search-recommendation-embedding
base: main
repo: jubishop/podhaven
---

# PR Review #473

0 Critical, 2 Warning, 3 Nit | 5 fixed, 0 skipped, 0 pending | last session 2026-06-15

## Sessions

### 2026-06-14T23:08:38-07:00

- Fresh review of single-commit perf refactor (3 files). CodeRabbit rate-limited, no bot findings.
- F1 fixed in PR with a proven-failing regression test (scoring close→open re-drain).

### 2026-06-15T08:23:08-07:00

- Second fresh round. F1 re-verified fixed, not regressed. CodeRabbit still rate-limited (no review); no threads/reviews.
- F2 fixed in PR: restored the `isDetached()` bail before the hoisted podcast-context compute.
- F3 fixed in PR (user expanded scope): per-feed podcast-vector cache in `UnsavedEpisodeEmbeddingScorer`.
- Verified: lint pass, clean build (zero warnings), 77 tests across 9 suites green.

### 2026-06-15T08:50:05-07:00

- Third fresh round. F1/F2/F3 re-verified fixed, not regressed. CodeRabbit still rate-limited (rate-limit notice only; no review/threads).
- F4 fixed in PR: deleted the dead `embeddingVector(for:)` wrapper (zero production callers after the split); repointed the `unsavedSimilarityScores` test helper at the split methods.
- Verified: lint pass, clean build (zero warnings), 36 tests in 3 suites green (RecommendationSort, EmbeddingService, SearchRecommendationCollectorScoringAvailability).

### 2026-06-15T09:32:29-07:00

- Fourth fresh round. F1–F4 re-verified fixed against current code, none regressed; split is byte-identical (empty title→nil→no blend) and the `perf:` timing partitions cleanly (stages sum to total). No new defects.
- Flagged below-bar that the per-episode embed catch used closure `level: { _ in .info }` while the sibling podcast-context catch used bare `level: .info`; user opted to align them — F5 fixed in PR.
- PR feedback: zero threads/reviews; only the CodeRabbit rate-limit notice (no actual review ran), left as-is.
- Verified: lint pass, clean `build-for-testing` (zero warnings).

## Findings

### F5: Sibling embed catches used different caughtError level forms  (fixed)

- **severity:** Nit
- **status:** fixed
- **location:** PodHaven/Views/Search/Models/SearchPipelineRunner.swift:145
- **concern:** per-episode embed catch used closure `level: { _ in .info }` while the sibling podcast-context catch used bare `level: .info`
- **resolution:** switched the per-episode catch to bare `level: .info`. Behavior-identical (errors reaching it are always remarkable embedding errors — cancellation handled in a prior arm, no URLError path); bare form is the idiomatic default per the overload doc.
- **fixed_at:** 2026-06-15

### F4: `embeddingVector(for:)` is a test-only production surface  (fixed)

- **severity:** Nit
- **status:** fixed
- **location:** PodHaven/Recommendations/Embeddings/EmbeddingService.swift:166
- **concern:** embeddingVector(for:) had zero production callers after the split; only RecommendationSortTests called it
- **resolution:** deleted the wrapper; repointed the `unsavedSimilarityScores` test helper at podcastContextVector + episodeEmbeddingVector (byte-identical to what the wrapper did, so the sort suite still covers it).
- **fixed_at:** 2026-06-15

### F2: Hoisted podcast-context embed skips the `isDetached()` bail  (fixed)

- **severity:** Warning
- **status:** fixed
- **location:** PodHaven/Views/Search/Models/SearchPipelineRunner.swift:100
- **concern:** podcast-context vector computed before any isDetached() check
- **resolution:** added `if await isDetached() { return .cancelled }` before the hoisted podcast-context compute, mirroring the per-episode loop guard, so a detached feed bails before spending serialized-embedder time. Race not deterministically reproducible; collector suites green.
- **fixed_at:** 2026-06-15

### F3: Same once-per-podcast hoist available in UnsavedEpisodeEmbeddingScorer  (fixed)

- **severity:** Nit
- **status:** fixed
- **location:** PodHaven/Recommendations/ViewUtility/UnsavedEpisodeEmbeddingScorer.swift:51
- **concern:** scorer recomputes identical podcast vector per episode on cold cache
- **resolution:** per-feedURL podcast-vector cache in the loop (mirrors saved-side podcastVectorCache); switched to `podcastContextVector` + `episodeEmbeddingVector`. Byte-identical output, so existing scorer + sort suites cover it.
- **fixed_at:** 2026-06-15

### F1: Hoisted podcast-vector throw makes whole feed terminal `.failed`  (fixed)

- **severity:** Warning
- **status:** fixed
- **location:** PodHaven/Views/Search/Models/SearchPipelineRunner.swift
- **concern:** podcastContextVector throw failed the whole feed instead of per-episode skip
- **resolution:** catch the non-cancellation throw, log once at `.info`, finish `.success([])` so the feed stays recoverable (empty-`.scored`, re-drained on scoring reopen) instead of terminal `.failed`. Regression test added (`podcastEmbeddingFailureStaysRecoverable`) + `ScriptedEmbeddable.errorFor` hook.
- **fixed_at:** 2026-06-14
