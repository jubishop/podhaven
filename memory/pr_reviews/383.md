---
pr: 383
title: "perf(recommendations): score unsaved episodes off the main actor"
branch: worktree-scoreUnsavedBatch
base: main
repo: jubishop/podhaven
---

# PR Review #383

0 Critical, 0 Warning, 0 Nit | 0 fixed, 0 skipped, 0 pending | last session 2026-05-30

## Sessions

### 2026-05-30T15:29:33-07:00

Context: none supplied. First review pass. PR is MERGEABLE, not draft, all checks green
(Swift Tests, Python Tests, CodeRabbit). Diff is focused: replaces the synchronous
single-vector `RecommendationEngine.similarityScore(forEmbedding:)` with a batch
`@concurrent similarityScores(forEmbeddings:) -> [Float?]`, and updates the two call
sites (`UnsavedEpisodeEmbeddingScorer`, a `SortAndFilterTests` helper). Verified:
positional nil alignment, scratch buffer sized to `dim` matches the established
`scoreEpisodes` contract and `WhiteningTransform.apply`'s `destination.count == mean.count`
guard, cancellation preserved (`index % 64`), no remaining production callers of the
removed API, docs updated accurately, and three new behavioral tests cover cold-cache,
positional batch==single equivalence, and dimension-mismatch. No actionable findings.

## Findings

No actionable findings.
