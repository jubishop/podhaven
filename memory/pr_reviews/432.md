---
pr: 432
title: "✨ refactor(search): consolidate discovery list view model into single type"
branch: worktree-multiSelectSearch
base: main
repo: jubishop/podhaven
---

# PR Review #432

0 Critical, 1 Warning, 1 Nit | 2 fixed, 0 skipped, 0 pending | last session 2026-06-09

## Sessions

### 2026-06-09T08:17:40-07:00

- Re-review: fresh code round found nothing new. Verified targeted backing-owner removal, including the same-mediaGUID-across-feeds edge — `ScoredEpisode.feedURL` in synthesized equality keeps `discoveryListState` change-detection (and thus `syncEntries`/`backingFeedURLByMediaGUID`) correct when a hidden duplicate surfaces.
- Confirmed bulk-action hook (`didPerformBulkAction`) covers exactly the consuming set (play/queue/replace/rate/finish); caching/tag/dequeue stay non-consuming, mirroring the per-row hook.
- F1 (fixed) and F2 (fixed) re-verified against current code; both stay closed.
- PR feedback: no threads; 3 CodeRabbit passes already minimized (ledger-schema false positive, now durably excluded via `.coderabbit.yaml`); remaining open comment is the informational walkthrough. No GitHub writes needed.

### 2026-06-08T15:45:00-07:00

- Fresh review: clean refactor. F1 (Nit, low-confidence dup-id crash) fixed in PR.
- PR feedback: no threads/reviews; only CodeRabbit in-progress comment. Nothing to reconcile.
- Verified: SearchRecommendationCollectorRemovalTests (8) + SearchViewModelTests (16) green; lint clean.

### 2026-06-08T16:18:10-07:00

- F2 skipped: accepted as an extremely rare stale-cleanup edge; action still succeeds.

### 2026-06-08T18:28:38-07:00

- Re-review: fresh code round found nothing new; bulk-action hook coverage mirrors the per-row consuming set; IdentifiedArray migration + dedup verified.
- F1 (fixed) and F2 (skipped) re-verified against current code; both stay closed.
- CodeRabbit (2 review passes) only flagged the ledger against the memory-page schema — confirmed false positive; pr_reviews ledgers are exempt per memory/README.md. Minimized both; durable fix is a .coderabbit.yaml exclusion for pr_reviews/**.

### 2026-06-08T18:49:15-07:00

- F2 fixed in PR with source-level MediaGUID coalescing by recommendation rank plus targeted backing-owner removal.
- Verified: SearchRecommendationCollectorRemovalTests (9) green after reproducing the stale-pick failure before the fix; Ingest, TypedSearch, and ScoringAvailability collector suites also green.

## Findings

### F2: Duplicate discovery pick can stay stuck after action  (fixed)

- **severity:** Warning
- **status:** fixed
- **location:** PodHaven/Views/Search/Models/SearchRecommendationCollector.swift:219
- **concern:** removePick indexes one backing entry per mediaGUID while the discovery list hides duplicate mediaGUID rows
- **resolution:** coalesced each source projection to one pick per MediaGUID using rankComparator, and indexed backing picks by (feedURL, MediaGUID) so consuming actions remove only the visible representative's backing owner while preserving other feed owners for later narrowed searches.
- **fixed_at:** 2026-06-08

### F1: picks projection can crash on duplicate mediaGUID  (fixed)

- **severity:** Nit
- **status:** fixed
- **location:** PodHaven/Views/Search/Models/SearchDiscoveryListViewModel.swift:48
- **concern:** syncEntries built IdentifiedArray(uniqueElements:) from picks without dedup
- **resolution:** switched to IdentifiedArray(_:uniquingIDsWith:) keep-first, matching the codebase pattern; duplicate mediaGUIDs across entries now degrade to the higher-ranked pick instead of tripping the precondition. No regression test — a precondition crash aborts the test process, so red-green is impractical.
- **fixed_at:** 2026-06-08
