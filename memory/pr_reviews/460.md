---
pr: 460
title: "✨ feat(smart-lists): add title-or-description fields and uncached state; drop equals operator"
branch: worktree-titleOrDescriptionFilters
base: main
repo: jubishop/podhaven
---

# PR Review #460

0 Critical, 1 Warning, 2 Nit | 3 fixed, 0 skipped, 0 pending | last session 2026-06-13

## Sessions

### 2026-06-13T16:11:42-07:00

- Fresh code-only round: production code correct. v60 migration mirrors v59 string-replace (safe — JSONEncoder escapes quotes inside values, so `"op":"equals"` only ever a real operator token; no seed used equals). `isUncached`→`!Episode.cached` follows the `isUnrated`→`!Episode.rated` pattern; `cached = cachedFilename != nil` so the negation is null-safe. `titleOrDescription` uses `EpisodeFTS/PodcastFTS.matching()`, the same whole-table API already shipped in `Episode.matchesText`. No new production defects.
- Re-derived the live-search vs saved-filter divergence (`Episode.matchesText` still prefix/any-order/cross-entity); already decided last session (intentional split) — not re-asked.
- F3 (Nit, CR-raised) fixed: docs test-plan item #4 (Codable round-trip) omitted `titleOrDescription`/`isUncached` though the fixture exercises them; added both to match the updated item #3.
- PR feedback: both threads (docs test-plan, v60Tests dict) already resolved/outdated; remaining CR items are summary reviews + walkthrough only.

### 2026-06-13T15:13:09-07:00

- F1 fixed: contains/doesNotContain now phrase-match (FTS5Pattern matchingPhrase) — whole words / contiguous in-order phrase, no prefix. Phrases never span columns (verified via sqlite3), so titleOrDescription whole-table == OR-of-columns; F1 resolved without restructure. Rewrote containsWordPrefix→containsWholeWordsAndPhrases; added titleOrDescriptionPhraseDoesNotSpanColumns; both proven red against old engine, green after. Engine comment + docs updated.
- User Q on live filter (Episode.matchesText): explained prefix+any-order+cross-entity semantics; user chose to leave it loose (intentional split: live search loose, saved rules precise). No change.
- CodeRabbit docs test-plan thread: partly addressed via accurate docs edit (not CR's imprecise "OR of columns" wording); went outdated after push.
- F2 (Nit, CR-raised) fixed on user request: scoped v60Tests filter query to its own two rows, removing the Dictionary(uniqueKeysWithValues:) duplicate-title trap; thread replied + resolved. (Merged v59Tests still uses the unscoped pattern — out of PR scope, left as-is.)

## Findings

### F3: docs Codable test-plan omits new enum values  (fixed)

- **severity:** Nit
- **status:** fixed
- **location:** docs/initiatives/smart-lists.md:380
- **concern:** Codable round-trip bullet listed new condition kinds but omitted the new enum values (titleOrDescription, isUncached) the fixture exercises
- **resolution:** added both values to bullet #4; now matches item #3 and the actual fixture
- **source_review_id:** PRR_kwDONT4Ems8AAAABC8C2pA
- **source: coderabbitai**
- **fixed_at:** 2026-06-13

### F1: titleOrDescription whole-table match ≠ OR-of-columns for multi-token  (fixed)

- **severity:** Warning
- **status:** fixed
- **location:** PodHaven/Database/SmartListFilterEngine.swift:117,139
- **concern:** titleOrDescription whole-table FTS MATCH; multi-token values matched tokens split across title+description, unlike OR of two single-column conditions — contradicting the PR's "equivalent to OR-ing a title and a description condition" contract
- **resolution:** changed contains/doesNotContain from matchingAllPrefixesIn to matchingPhrase (per user: contains should match whole words / a contiguous phrase, no prefix). FTS5 phrases never span columns, so whole-table titleOrDescription now equals OR-of-columns; tests proven red-before-green against old engine
- **fixed_at:** 2026-06-13

### F2: v60Tests Dictionary(uniqueKeysWithValues:) can trap on duplicate titles  (fixed)

- **severity:** Nit
- **status:** fixed
- **location:** PodHavenTests/MigrationTests/v60Tests.swift:50
- **scope:** in-diff
- **concern:** dict built from SELECT over all smartList rows keyed by non-unique title; would trap if seeds/fixtures ever share a title
- **resolution:** scoped the SELECT to the test's own two rows (WHERE title IN ('Equals','LiteralValue')); keys now test-controlled and unique
- **source_thread_id:** PRRT_kwDONT4Ems6JW7Sl
- **source: coderabbitai**
- **fixed_at:** 2026-06-13
