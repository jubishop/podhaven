---
pr: 390
title: Fix Sentry log environment mismatch on TestFlight
branch: worktree-fixSentryEnvMismatch
base: main
repo: jubishop/podhaven
---

# PR Review #390

0 Critical, 0 Warning, 2 Nit | 2 fixed, 0 skipped, 0 pending | last session 2026-06-01

## Sessions

### 2026-05-31T22:30:42-07:00

- Validated core fix against Sentry Cocoa 9.15.0 source: `sentry.environment` is the reserved log facet key; the log scope applier freezes `options.environment` at start; `beforeSendLog` runs after `applyScope`, so the override correctly fixes the facet. Fix is correct.
- CodeRabbit thread-safety thread verified as non-issue (`AppInfo.environment` is `ThreadSafe`); not re-raised, left open.
- F1 fixed: made helper `private`, deleted isolated test. F2 fixed: removed redundant custom `environment` attribute.
- Validation: swift-format + bin/lint-swift-format pass; no stale refs; full local build declined by user (CI Swift Tests running).

## Findings

### F1: Regression test doesn't guard the actual fix; forces non-private visibility

- **severity:** Nit | **status:** fixed
- **location:** PodHavenTests/SentryEnvironmentTests.swift (deleted); PodHaven/AppLauncher.swift:233
- **concern:** The test exercised `sentryLogApplyingCurrentEnvironment` in isolation, only asserting the attribute transform, not the `options.beforeSendLog` wiring (the real regression vector); helper was `internal` solely for the test.
- **outcome:** Made `sentryLogApplyingCurrentEnvironment` `private`; deleted SentryEnvironmentTests.swift (no Sentry harness for an integration-level test).
- **resolution:** fixed — removed test-only surface; no replacement test per user (integration harness not worth it)
- **fixed_at:** 2026-06-01

### F2: Redundant custom `environment` log attribute now duplicates `sentry.environment`

- **severity:** Nit | **status:** fixed
- **location:** PodHaven/Logging/Handlers/SentryLogHandler.swift:35
- **concern:** `SentryLogHandler` attached a custom `"environment"` attribute (= AppInfo.environment at emit time) that, after this PR's `beforeSendLog` fix to the reserved `sentry.environment` facet, became redundant duplicate data on every log.
- **outcome:** Removed the custom `environment` attribute; logs now rely on the `sentry.environment` facet.
- **resolution:** fixed
- **fixed_at:** 2026-06-01
