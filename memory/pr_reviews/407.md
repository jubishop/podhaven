---
pr: 407
title: Notify Broadcast SwiftUI observers synchronously on the main actor
branch: worktree-notifyObserversSync
base: main
repo: jubishop/podhaven
---

# PR Review #407

0 Critical, 0 Warning, 0 Nit | clean review, no findings | last session 2026-06-02

## Sessions
### 2026-06-02
- Fresh code-only review of Broadcast sync-notify change + EpisodeSwipeSettingsViewModel computed pass-through revert. No findings.
- Verified: `MainActor.assumeIsolated` safe whenever `Thread.isMainThread` (same `pthread_main_np` check under default executor); ThreadSafe lock released before `notifyObservers`, so sync observer callbacks can't deadlock; in-view Broadcast writers (PlayBar/PlayBarSheet/PodcastSettingsView) are all `#Preview`/`.task` code, not production view bodies — no "modifying state during view update" regression.
- Both regression tests (`BroadcastTests.notifiesObserversSynchronouslyOnMain`, `EpisodeSwipeSettingsViewModelTests.mutationsNotifySynchronously`) genuinely fail against the old async-hop code.
- CodeRabbit: no actionable comments. No open review threads.

## Findings
None.
