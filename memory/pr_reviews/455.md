---
pr: 455
title: "⚡️ perf(episodes): speed up smart list filtering with indexes and FTS"
branch: speed-up-filters
base: main
repo: jubishop/podhaven
---

# PR Review #455

0 Critical, 3 Warning, 0 Nit | 3 fixed, 0 skipped, 0 pending | last session 2026-06-13

## Sessions

### 2026-06-13T12:56:16-07:00

- Independent third `/review` pass on HEAD 494870ac: no new findings; converges with the prior two passes. F1–F3 remain fixed.
- Ran targeted suites on My Mac — 22/22 green (SmartListFilterEngine, AppDBOptimize, V59Migration), incl. textFieldScoping (proves FTS column-scoped MATCH), containsWordPrefix/Untokenizable, doesNotContain null-safety, optimizeChecksAllTables, parityWithHardcodedDefaults.
- Re-verified: COALESCE-drop safe (sqlFilter co-filters non-null), partial finishDate/queueDate indexes valid+usable, pubDate index pre-exists (no sort-index gap), optimize mask 0x1fffe correct, no stray startsWith. contains substring→word-prefix change is documented/rationalized in PR body + research note — not raised. CodeRabbit rate-limited (0 actionable); 0 open threads/reviews.

### 2026-06-13T11:44:02-07:00

- F3 fixed in PR: forced the lifecycle optimize pass to check every table while preserving SQLite's bounded default optimize behavior and the configured analysis limit; added AppDBOptimizeTests coverage for stale stats with no connection-local query history.

### 2026-06-13T11:33:24-07:00

- Independent second `/review` pass on HEAD e61a4e17: no new findings. Re-verified F1/F2 hold; additionally confirmed the partial finishDate/queueDate indexes serve sort filter+order (sqlFilter co-filters non-null), FTS column-scoping (CI textFieldScoping), v48 rowid==id mapping, doesNotContain `NOT IN` null-safety, no index redundancy, no stray startsWith refs. CodeRabbit re-ran clean (no actionable comments).

### 2026-06-13T09:01:02-07:00

- Fresh review: no correctness defects. v59 REPLACE (compact-JSON safe), coalesce-drop (sqlFilter guards non-null), FTS swap (rides existing matchesText observation pattern), and doesNotContain NOT-IN null-safety all verified sound. CodeRabbit rate-limited — no bot findings.
- F1 fixed in PR: refreshed smart-lists.md (removed startsWith TextOp; rewrote contains/doesNotContain prose from LIKE → column-scoped FTS5; updated test-plan line).
- F2 fixed in PR: per-connection PRAGMA analysis_limit in prepareDatabase + AppDB.optimize() run on .background via new DatabaseMaintainer (BackgroundTask-wrapped). New AppDBOptimizeTests. User chose on-background trigger over BGTask.

## Findings

### F3: Background optimize can skip reader-only Smart List tables  (fixed)

- **severity:** Warning
- **status:** fixed
- **location:** PodHaven/Database/AppDB.swift:238
- **concern:** AppDB.optimize ran plain PRAGMA optimize on the writer connection, so tables queried only by reader connections could be skipped once sqlite_stat1 already existed.
- **resolution:** switched optimize to run outside a wrapping transaction with the default optimize mask plus all-table eligibility (`0x1fffe`), preserving the bounded analysis limit; added AppDBOptimizeTests coverage for a stale sqlite_stat1 row that only refreshes with the forced mask.
- **fixed_at:** 2026-06-13

### F1: Design doc still described removed startsWith / old LIKE contains  (fixed)

- **severity:** Warning
- **status:** fixed
- **location:** docs/initiatives/smart-lists.md:64,93-96,379
- **concern:** smart-lists.md documented startsWith TextOp + LIKE/like(_:escape:) contains semantics that this PR removed/changed
- **resolution:** rewrote the TextOp declaration, the episode/podcast text-matching prose (column-scoped FTS5, untokenizable→matchNone/noOp, null-safe doesNotContain), and the filter-engine test-plan line to match the shipped engine
- **fixed_at:** 2026-06-13

### F2: Planner stats only refreshed once; no periodic PRAGMA optimize  (fixed)

- **severity:** Warning
- **status:** fixed
- **location:** PodHaven/Database/AppDB.swift; PodHaven/Database/DatabaseMaintainer.swift; PodHaven/PodHavenApp.swift
- **concern:** v59 ran ANALYZE once; no PRAGMA optimize so sqlite_stat1 drifts as episodes accumulate, eroding the index win
- **resolution:** set per-connection analysis_limit (bounded, cheap) in prepareDatabase; added AppDB.optimize() running PRAGMA optimize on the writer; new DatabaseMaintainer runs it on every .background transition (mobile analog of connection-close, no cold launch needed) wrapped in a BackgroundTask. AppDBOptimizeTests covers analysis_limit wiring + optimize success.
- **fixed_at:** 2026-06-13
