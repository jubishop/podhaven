---
pr: 431
title: "♻️ refactor(search): eliminate view model lifecycle teardown for persistent collector state"
branch: worktree-keepSearchNav
base: main
repo: jubishop/podhaven
---

# PR Review #431

0 Critical, 3 Warning, 1 Nit | 4 fixed, 0 skipped, 0 pending | last session 2026-06-08

## Sessions

### 2026-06-08T14:09:27-07:00

- Fresh re-review (code only) post-F4 fix; no new findings. Re-verified F1–F4 still fixed against current code; teardown removal complete/consistent (long-lived view model via TabView `@State` in ContentView; `appear()` retry on `.error`/`.idle` + stale-query guard sound; no dangling `reset()`/`disappear()` refs; collector tests free of `reset()`).
- Convergence: F4 CodeRabbit thread resolved + bot-confirmed (commit 73fc0dd); latest CR review (post-fix) returned "No actionable comments"; open thread count 0. Ledger schema nit remains correctly skipped (review-tool artifact, not a memory page).

### 2026-06-08T13:51:15-07:00

- F4 fixed in PR with proven-red regression test; failed-feed retry candidate left dropped per prior ledger decision.

### 2026-06-08T13:19:55-07:00

- Fresh re-review (code only). Independently reached the same conclusions: teardown removal is complete and consistent (no dangling `reset()`/`disappear()` refs; `visiblePicks` is a live test helper; deleted teardown tests were `reset()`-only, cancellation-on-query-change still covered by TypedSearch tests). `appear()` no-op-on-loaded is correct given nothing is torn down.
- Re-verified F1/F2/F3 still fixed in current code; no regressions; no new findings. Failed-feed-retry candidate dedup'd against F2 (intentionally dropped).
- PR feedback: one minimized CodeRabbit review (ledger frontmatter schema nit on a review-tool artifact — skipped, minimized) + benign walkthrough summary; no open threads.

### 2026-06-08T12:41:51-07:00

- Fresh review (code only). SearchViewModel long-lived premise verified (TabView @State). No ledger/threads/reviews; one benign CodeRabbit summary comment (no actionable items).
- F1 (Warning) fixed in PR with proven-red regression test; F2 (Nit) fixed in PR (comments only).

### 2026-06-08T12:56:26-07:00

- F3 fixed in PR with proven-red regression test; focused `SearchViewModelTests` and swift-format lint passed.

## Findings

### F4: Stale errored search can retry after the visible query changed  (fixed)

- **severity:** Warning
- **status:** fixed
- **location:** PodHaven/Views/Search/Models/SearchViewModel.swift:267
- **concern:** appear() retries an errored debounced query without checking the live search text still matches
- **resolution:** appear() now gates stale `.idle`/`.error` typed-search retries on trimmed `searchText == searchedText`; regression test `returningAfterSearchErrorWithEditedQueryDoesNotRetryStaleSearch` proven red before fix, green after.
- **fixed_at:** 2026-06-08

### F3: Errored typed search not auto-retried on tab re-entry  (fixed)

- **severity:** Warning
- **status:** fixed
- **location:** PodHaven/Views/Search/Models/SearchViewModel.swift (appear)
- **concern:** appear() returns for any non-empty typed search, including searchState == .error
- **resolution:** appear() now retries `.idle`/`.error` typed searches and stays no-op for `.loading`/`.loaded`. Regression test `returningAfterSearchErrorRetriesLoad` proven red (11.3s timeout) before fix, green after.
- **fixed_at:** 2026-06-08

### F1: Errored trending not auto-retried on tab re-entry  (fixed)

- **severity:** Warning
- **status:** fixed
- **location:** PodHaven/Views/Search/Models/SearchViewModel.swift (appear)
- **concern:** appear() only fetched when state == .idle, so an .error section never retried on return
- **resolution:** appear() now switches on state and re-fetches on .idle/.error (no-op for .loading/.loaded). Regression test `returningAfterTrendingErrorRetriesLoad` proven red (11.3s timeout) before fix, green after.
- **fixed_at:** 2026-06-08

### F2: Stale comments reference removed reset()/teardown  (fixed)

- **severity:** Nit
- **status:** fixed
- **location:** PodHaven/Views/Search/Models/SearchRecommendationCollector.swift (shouldDrain, reconcileAndIngest, ensureDrainTaskRunning)
- **concern:** comments described a reset()/leave-return recovery path that no longer exists
- **resolution:** rewrote the three comments to match current behavior (failed feeds terminal for the collector's lifetime; debouncer-cancel guard; first-run stream replay). Failed-feed retry intentionally left dropped (low value).
- **fixed_at:** 2026-06-08
