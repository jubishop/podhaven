---
pr: 387
title: Add combined "Now Playing & Up Next" home-screen widget
branch: worktree-mediumWidget
base: main
repo: jubishop/podhaven
---

# PR Review #387

0 Critical, 0 Warning, 5 Nit | 5 fixed | last session 2026-06-01

## Sessions

### 2026-06-01T09:24:45-07:00

- Fresh full re-read at HEAD 420720ae (the F4 fix, which landed after last session's read at 237bbce8). No new findings; F1–F4 all verified fixed with no regression.
- F3 (widgetFallback) and F4 (QueueEntry.items) confirmed pure behavior-preserving extractions: switch in PlaybackStatus.widgetFallback is byte-identical to the old inline versions; both queue providers now call QueueEntry.items(from:). Swept for stragglers — queue mapping/usShort/compactReadableFormat/decodeArtwork exist only in the shared helper; widgetFallback only in PlaybackStatus.
- Deep links (now-playing/episode, queue/<id>, queue, now-playing) all resolve in WidgetService; bundle registers NowPlayingQueueWidget; combined-widget header has no widget-wide URL by design (interactive systemLarge with per-element Links/Buttons).
- PR feedback all resolved/tracked: F1 thread resolved+outdated, F4 outside-diff review addressed by 420720ae. CodeRabbit still processing the F4 commit at intake.
- F5 (user-proposed, fixed): added widget-wide .widgetURL("podhaven://widget/queue") to the combined widget so dead-zone taps route to Up Next, matching the standalone queue widget. Confirmed via existing medium now-playing widget that widgetURL coexists with per-element Links/Buttons on systemLarge. swift-format + lint pass; PodHavenWidget builds clean on My Mac (zero warnings).

### 2026-06-01T00:00:00-07:00

- Fresh full re-read at HEAD 237bbce8. F1–F3 verified still fixed (no regression). All 8 reload sites include nowPlayingQueueKind; widgetFallback used identically by both providers; deep links (now-playing/episode, queue/<id>) resolve in WidgetService; FakeWidgetCenter change has no stale refs.
- New F4: snapshot→QueueEntryItem map duplicated verbatim in QueueProvider.makeEntry (QueueWidget:34-45) and NowPlayingQueueProvider.readQueue (NowPlayingQueueWidget:112-123). Matches open CodeRabbit outside-diff comment.

### 2026-05-31T22:45:00-07:00

- Re-read after nit fixes landed (2bb27d35); protocol rename to Set<String> verified; F3 fixed (PlaybackStatus.widgetFallback extraction, both providers call it).

### 2026-05-31T21:02:52-07:00

- Initial full review (no Critical/Warning); F1 fixed (rawImage empty states) and F2 fixed (dropped unused queueTotalCount).

## Findings

### F1: Empty-state icon bypasses AppIcon abstraction

- **severity:** Nit | **status:** fixed
- **location:** PodHavenWidget/Queue/QueueListView.swift:14
- **concern:** Empty-state queue icon built via Image(systemName: AppIcon.upNext.systemImageName) instead of the AppIcon image abstraction, violating the *View.swift AppIcon guideline.
- **outcome:** Switched to AppIcon.upNext.rawImage in QueueListView and AppIcon.noEpisodeSelected.rawImage in NowPlayingWidgetView.emptyState; preserves the muted foregroundStyle. CodeRabbit's .image fix rejected (would force accentColor).
- **resolution:** fixed via rawImage (compliant accessor)
- **fixed_at:** 2026-05-31
- **source_thread_id:** PRRT_kwDONT4Ems6F_LIw

### F2: queueTotalCount carried but never displayed

- **severity:** Nit | **status:** fixed
- **location:** PodHavenWidget/NowPlayingQueue/NowPlayingQueueWidget.swift:115 / NowPlayingQueueEntry.swift:14
- **concern:** readQueue() returns totalCount and makeEntry threads queueTotalCount into NowPlayingQueueEntry (3 sites), but NowPlayingQueueWidgetView never renders it.
- **outcome:** Removed queueTotalCount field, all construction sites, the preview arg, and readQueue's totalCount return (now returns [QueueEntryItem]).
- **resolution:** fixed (dead state removed); standalone QueueEntry.totalCount left unused but out-of-scope
- **fixed_at:** 2026-05-31

### F3: Timeline/fallback scaffolding duplicated across the two now-playing providers

- **severity:** Nit | **status:** fixed
- **location:** PodHavenWidget/NowPlayingQueue/NowPlayingQueueWidget.swift:34 (+ NowPlaying/NowPlayingWidget.swift:29)
- **concern:** NowPlayingQueueProvider's getTimeline duplicates NowPlayingProvider's fallback-status switch (playing/waiting→paused, loading→stopped, else nil) verbatim; the mapping can drift between widgets.
- **outcome:** Extracted the active-state→fallback mapping to PlaybackStatus.widgetFallback (main declaration, shared widget-target file); both providers now call entry.playbackStatus.widgetFallback.
- **resolution:** fixed (shared helper removes the drift-prone copy)
- **fixed_at:** 2026-05-31

### F4: snapshot→QueueEntryItem mapping duplicated across the two queue providers

- **severity:** Nit | **status:** fixed
- **location:** PodHavenWidget/Queue/QueueEntry.swift (helper) (+ QueueWidget.swift:34, NowPlayingQueueWidget.swift:112)
- **concern:** NowPlayingQueueProvider.readQueue's snapshot.queue.map → QueueEntry.QueueEntryItem block is byte-for-byte identical to QueueProvider.makeEntry's; the two can drift on formatting/decoding (usShort, compactReadableFormat, decodeArtwork).
- **outcome:** Extracted `QueueEntry.items(from: QueueSnapshot)`; both providers now call it. Pure refactor, no behavior change. swift-format + lint pass; PodHavenWidget builds clean (zero warnings).
- **resolution:** fixed (shared two-call-site helper removes the copy-paste)
- **fixed_at:** 2026-06-01
- **source_review_id:** PRR_kwDONT4Ems8AAAABBi35Lw

### F5: Combined widget has no widget-wide tap fallback

- **severity:** Nit | **status:** fixed
- **location:** PodHavenWidget/NowPlayingQueue/NowPlayingQueueWidget.swift:122
- **concern:** NowPlayingQueueWidget (systemLarge) sets no .widgetURL, so dead-zone taps (header title text, dividers, empty space below a short queue) do nothing, unlike the standalone now-playing/queue widgets.
- **outcome:** Added .widgetURL("podhaven://widget/queue") to the widget content so dead-zone taps route to Up Next; per-element Links/Buttons keep precedence on systemLarge (proven by the existing medium now-playing widget that combines widgetURL + Link + intents). User-proposed enhancement.
- **resolution:** fixed (one-line widget-wide fallback; routing already covered by WidgetService queue→showUpNext)
- **fixed_at:** 2026-06-01
