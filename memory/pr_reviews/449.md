---
pr: 449
title: Unify delete confirmations on alerts and fix smart list swipe flicker
branch: unify-delete-confirmations
base: main
repo: jubishop/podhaven
---

# PR Review #449

0 Critical, 0 Warning, 1 Nit | 1 fixed, 0 skipped, 0 pending | last session 2026-06-12

## Sessions

### 2026-06-12T22:11:52-07:00

- F1 fixed in PR; aligned Smart Lists docs with custom swipe delete behavior.

### 2026-06-12T22:05:20-07:00

- Fresh code-only round: no actionable findings. Smart-list delete (hub swipe + editor sheet) now routes through the injected `alert`, mirroring the accepted tag-deletion pattern exactly (`deleteSmartList`/`performDeleteSmartList` ↔ `deleteTag`/`performDeleteTag`); single "confirms before deleting" alert test matches house norm.
- Verified alert-over-sheet wiring is in place (root `customAlert` gated on `sheet.config == nil`, sheet presents its own via `alert:` param) so the editor sheet's Delete confirmation surfaces.
- Custom red-tinted `AppIcon.delete` swipe (no `.onDelete`, no `role: .destructive`) genuinely fixes the flicker — no optimistic row removal; row animates out only after the persisted delete.
- Tag-icon swap `removeTag`→`delete` is semantically correct; `AppIcon.removeTag` still used by TagsView/TagMenu (not dead).
- PR feedback clean: no threads, no reviews; lone CodeRabbit walkthrough comment already minimized, zero inline findings. Nothing to close.

## Findings

### F1: Smart Lists doc still mentions edit-mode delete  (fixed)

- **severity:** Nit
- **status:** fixed
- **location:** docs/initiatives/smart-lists.md:7
- **concern:** Smart Lists initiative summary and sample still imply edit-mode/on-sample delete after the PR moved hub deletion to swipe-only
- **fixed_at:** 2026-06-12
