---
pr: 413
title: Persist Up Next recommendation pool so it isn't empty on cold launch
branch: worktree-persistUpNextRecs
base: main
repo: jubishop/podhaven
---

# PR Review #413

0 Critical, 0 Warning, 0 Nit | clean review, no findings | last session 2026-06-02

## Sessions
### 2026-06-02
- Full fresh review. No production findings. CodeRabbit reported no actionable comments.
- Traced cold-launch lifecycle: GRDB observation → cache rebuild → recommendations rebuild replaces the persisted seed; per-episode staleness filtered by `Episode.candidate` / `isCandidate` in both consumers (UpNextViewModel, PlayManager).
- Confirmed `.equatable` dedup policy preserved (DefaultsStorable refines Equatable); pool bounded to 100 IDs; FakeKeyValueStore isolates tests.
- Omitted: new test's implicit "sharedState resolved after store write" ordering dep — documented, fails-safe, validates real behavior.

## Findings

(none)
