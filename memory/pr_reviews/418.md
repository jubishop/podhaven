---
pr: 418
title: Cache auto-inferred FreshnessCadence in a stored column
branch: worktree-cacheAutoFreshness
base: main
repo: jubishop/podhaven
---

# PR Review #418

0 Critical, 0 Warning, 0 Nit | 0 fixed, 0 skipped, 1 withdrawn | last session 2026-06-03

## Sessions
### 2026-06-03
- Fresh review. F1 (sparse-cadence divergence) investigated, TDD'd, then withdrawn: the live path's "store .default for sparse" is intentional and load-bearing (two pre-existing Observatory tests depend on it). Changes reverted. No actionable findings. PR feedback: no threads/reviews; CodeRabbit "no actionable comments"; its docstring-coverage check contradicts the repo no-doc-comment rule (ignored).

## Findings

### F1: Live recompute stores `.default` for sparse podcasts; migration stores NULL  (withdrawn)
- **severity:** Warning (as filed)
- **location:** PodHaven/Database/RecommendationRepo.swift:137-138
- **concern:** updateInferredFreshnessCadence writes inferred=.default(weekly) for 1-2 episode podcasts; v50 backfill leaves them NULL
- **resolution:** Not a bug. Storing `.default` for sparse is deliberate and tested: `ObservatoryScoringContextInputsTests.defaultCadenceWhenTooFewEpisodes` asserts sparse podcasts are PRESENT in the map as `.default`, and `skipsIrrelevantChanges` relies on inferred==weekly so setting a manual weekly override is a no-op (no re-emission). A TDD "fix" to store nil broke both. Migration's NULL-for-sparse is the only outlier but is immaterial: resolves identically via `?? .default`, converges to weekly on first refresh, and the deferred Settings-UI would match the tested default contract anyway. Reverted all changes.
